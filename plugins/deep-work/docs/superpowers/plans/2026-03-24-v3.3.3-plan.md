# Deep-Work v3.3.3 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SKILL.md 트리거 평가 최적화 + Profile System 멀티 프리셋 지원 (v3.3.3)

**Architecture:** Claude Code 플러그인 프로젝트로, 코드는 Markdown 명령어 파일과 JSON 설정으로 구성. trigger-eval.json으로 트리거 정확도를 검증하고, 명령어 파일(deep-work.md, deep-resume.md)의 프롬프트 로직을 수정하여 멀티 프리셋을 지원한다.

**Tech Stack:** Markdown (command prompts), YAML (profile), JSON (trigger-eval, plugin.json, package.json)

**Spec:** `docs/superpowers/specs/2026-03-24-v3.3.3-design.md`

**Project Root:** `/Users/sungmin/.claude/plugins/deep-work/plugins/deep-work/`

---

## 파일 구조

| 파일 | 역할 | 변경 유형 |
|------|------|----------|
| `deep-work-workflow-workspace/trigger-eval.json` | 트리거 평가 테스트 케이스 | 수정 (20개 → 31개) |
| `skills/deep-work-workflow/SKILL.md` | 스킬 description + 본문 | 수정 (description 축소, v3.3.3 섹션 추가) |
| `commands/deep-work.md` | 세션 초기화 명령어 | 수정 (Step 1.5 멀티 프리셋 로직) |
| `commands/deep-resume.md` | 세션 재개 명령어 | 수정 (preset 필드 표시) |
| `.claude-plugin/plugin.json` | 플러그인 매니페스트 | 수정 (버전 + description) |
| `package.json` | npm 메타데이터 | 수정 (버전) |

---

## Task 1: trigger-eval.json 확장

**Files:**
- Modify: `deep-work-workflow-workspace/trigger-eval.json`

- [ ] **Step 1: 현재 trigger-eval.json 읽기**

파일 경로: `/Users/sungmin/.claude/plugins/deep-work/plugins/deep-work/deep-work-workflow-workspace/trigger-eval.json`

현재 상태 확인: 10개 true + 10개 false = 20개

- [ ] **Step 2: should_trigger=true 쿼리 추가**

기존 10개에 다음 6개 추가 (v3.3.2 키워드 커버리지):

```json
{
  "query": "프로필로 deep work 시작할게. 기본 프리셋 써줘",
  "should_trigger": true
},
{
  "query": "프리셋 설정해줘. dev랑 quick 모드 두 개 만들고 싶어",
  "should_trigger": true
},
{
  "query": "아까 작업하다 끊겼는데 세션 재개해줘",
  "should_trigger": true
},
{
  "query": "이어서 작업하고 싶어. 체크포인트에서 재개할 수 있어?",
  "should_trigger": true
},
{
  "query": "resume my deep work session from the last checkpoint",
  "should_trigger": true
},
{
  "query": "deep work 빠른 시작으로 해줘. quick 프리셋 있으면 그걸로",
  "should_trigger": true
}
```

- [ ] **Step 3: should_trigger=false 쿼리 추가**

기존 10개에 다음 5개 추가 (동음이의어 false positive 방지):

```json
{
  "query": "profile picture를 변경하는 API 엔드포인트 만들어줘",
  "should_trigger": false
},
{
  "query": "이력서(resume) 템플릿을 React 컴포넌트로 만들어줘",
  "should_trigger": false
},
{
  "query": "git checkpoint 태그를 자동으로 만드는 스크립트 짜줘",
  "should_trigger": false
},
{
  "query": "deep copy를 구현해야 하는데, structuredClone이랑 JSON.parse 중 뭐가 나아?",
  "should_trigger": false
},
{
  "query": "work directory를 /tmp로 변경하는 설정 알려줘",
  "should_trigger": false
}
```

- [ ] **Step 4: JSON 유효성 확인**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && python3 -c "import json; json.load(open('deep-work-workflow-workspace/trigger-eval.json')); print('Valid JSON, count:', len(json.load(open('deep-work-workflow-workspace/trigger-eval.json'))))"
```

Expected: `Valid JSON, count: 31`

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && git add deep-work-workflow-workspace/trigger-eval.json && git commit -m "feat(v3.3.3): trigger-eval.json 확장 (20→31개, profile/resume/checkpoint 커버리지)"
```

---

## Task 2: SKILL.md description 축소 + v3.3.3 섹션 추가

**Files:**
- Modify: `skills/deep-work-workflow/SKILL.md`

- [ ] **Step 1: SKILL.md frontmatter의 description 수정**

현재 description (SKILL.md:4-14):

```yaml
description: |
  This skill should be used when the user wants to follow a structured, phase-based
  development workflow that strictly separates research, planning, implementation, and
  testing. It applies when users say things like "deep work", "plan before code",
  "structured workflow", "research then plan then implement", "기획과 코딩 분리",
  "분석 후 구현", "계획 세우고 구현", "제로베이스", "from scratch", "quality gate",
  "품질 게이트", "SOLID review", "drift check", "deep-insight", "코드 메트릭",
  or when the user describes a complex, multi-file task that would benefit from
  structured planning to avoid premature implementation, architecture ignorance,
  or duplicate code. Also triggers on "resume session", "이어서", "세션 재개",
  "프로필", "profile", "quick start", "빠른 시작".
```

변경할 description:

```yaml
description: |
  This skill should be used when the user wants to follow a phase-based deep work
  workflow that separates research, planning, implementation, and testing. It applies
  when users say things like "deep work", "plan before code", "기획과 코딩 분리",
  "분석 후 구현", "계획 세우고 구현", "제로베이스", "from scratch", "quality gate",
  "SOLID review", "drift check", "deep-insight", "코드 메트릭", "preset", "프리셋",
  "resume session", "세션 재개", "이어서", "프로필", "빠른 시작",
  or when the user describes a complex, multi-file task that would benefit from
  structured planning before implementation.
```

변경 요점:
- `"structured workflow"` 제거 (너무 범용적, 다른 플러그인과 충돌)
- `"research then plan then implement"` 제거 (중복)
- `"품질 게이트"` 제거 (영문 "quality gate"만 유지)
- `"profile"`, `"quick start"` 제거 (영문 일반 단어, false positive 유발)
- `"preset"`, `"프리셋"` 추가 (v3.3.3 신규 기능)
- 후반부 설명 축소 ("avoid premature implementation, architecture ignorance, or duplicate code" → "structured planning before implementation")

- [ ] **Step 2: version 필드 업데이트**

SKILL.md:3의 `version: "3.3.2"` → `version: "3.3.3"`

- [ ] **Step 3: v3.3.3 Features 섹션 추가**

SKILL.md 하단, "v3.3.2 Features" 섹션 뒤에 추가:

```markdown
## v3.3.3 Features

### Multi-Preset Profile System
Named presets for different work styles — quick setup, review-focused, etc.
- Profile v2 format with `presets:` key (single YAML file)
- Auto-migration from v1 (existing single profile → `default` preset)
- `/deep-work --setup` for preset management (create, edit)
- `/deep-work --profile=X "task"` for direct preset selection
- Interactive preset selection via AskUserQuestion when multiple presets exist

### Trigger Evaluation Optimization
Expanded test cases and refined description for better trigger accuracy.
- trigger-eval.json expanded from 21 to 32+ queries
- Description keywords optimized (reduced false positives)
- Coverage for v3.3.2 features (profile, resume, checkpoint, preset)
```

- [ ] **Step 4: Profile System 섹션 업데이트**

SKILL.md:251-260의 "Profile System" 섹션을 업데이트하여 멀티 프리셋 내용 반영:

현재:
```markdown
## Profile System

On the first `/deep-work` run, you answer setup questions (mode, model routing, notifications, etc.) as usual. Your answers are automatically saved to `.claude/deep-work-profile.yaml`. On subsequent runs, the profile is loaded and **all setup questions are skipped** — you only provide the task description.

**Flags** override profile values for a single session:
- `/deep-work --team "task"` — use Team mode this time
- `/deep-work --zero-base "task"` — zero-base project this time
- `/deep-work --skip-research "task"` — skip to Plan phase
- `/deep-work --no-branch "task"` — no git branch this time
- `/deep-work --setup` — re-run all setup questions and update the profile
```

변경:
```markdown
## Profile System

On the first `/deep-work` run, you answer setup questions (mode, model routing, notifications, etc.) as usual. Your answers are automatically saved to `.claude/deep-work-profile.yaml` as the `default` preset. On subsequent runs, the profile is loaded and **all setup questions are skipped** — you only provide the task description.

**Multi-preset support (v3.3.3)**: Create named presets for different work styles (e.g., `dev`, `quick`, `review`). When multiple presets exist, you'll be asked to choose one at session start. All presets are stored in a single YAML file.

**Flags** override profile values for a single session:
- `/deep-work --profile=quick "task"` — use a specific preset
- `/deep-work --team "task"` — use Team mode this time
- `/deep-work --zero-base "task"` — zero-base project this time
- `/deep-work --skip-research "task"` — skip to Plan phase
- `/deep-work --no-branch "task"` — no git branch this time
- `/deep-work --setup` — manage presets (create, edit)
```

- [ ] **Step 5: 커밋**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && git add skills/deep-work-workflow/SKILL.md && git commit -m "feat(v3.3.3): SKILL.md description 최적화 + v3.3.3 기능 섹션 추가"
```

---

## Task 3: trigger-eval 검증 (skill-creator eval)

**Files:**
- Reference: `deep-work-workflow-workspace/trigger-eval.json`
- Reference: `skills/deep-work-workflow/SKILL.md`

- [ ] **Step 1: skill-creator 스킬로 트리거 정확도 평가**

`/skill-creator` 스킬을 실행하여 trigger-eval.json 기반 정확도 측정.
목표: 90%+ 정확도.

- [ ] **Step 2: 결과에 따라 description 미세조정**

90% 미달 시:
- false positive가 많으면 → description에서 해당 키워드 제거 또는 구체화
- false negative가 많으면 → description에 누락된 키워드 추가
- trigger-eval.json의 should_trigger 판정이 잘못된 쿼리 수정

조정 후 다시 eval 실행 → 90%+ 달성까지 반복.

- [ ] **Step 3: 최종 결과 커밋**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && git add skills/deep-work-workflow/SKILL.md deep-work-workflow-workspace/trigger-eval.json && git commit -m "feat(v3.3.3): 트리거 정확도 검증 완료 (목표 90%+)"
```

---

## Task 4: commands/deep-work.md — 멀티 프리셋 로직

**Files:**
- Modify: `commands/deep-work.md`

이것이 v3.3.3의 핵심 변경. Step 1.5를 전면 재작성한다.

- [ ] **Step 1: Step 1.5a에 `--profile=X` 플래그 추가**

`commands/deep-work.md:46-51`의 플래그 테이블에 행 추가:

현재:
```markdown
| Flag | Effect |
|------|--------|
| `--setup` | Force profile re-setup (all questions asked, profile overwritten) |
| `--team` | Override `team_mode` to `"team"` for this session only |
| `--zero-base` | Override `project_type` to `"zero-base"` for this session only |
| `--skip-research` | Override `start_phase` to `"plan"` for this session only |
| `--no-branch` | Override `git_branch` to `false` for this session only |
```

추가:
```markdown
| `--profile=X` | Use preset named `X` directly (skip interactive selection) |
```

- [ ] **Step 2: Step 1.5b `--setup` 동작 변경**

`commands/deep-work.md:57-65`를 수정. `--setup`이 태스크 없이 호출되면 프리셋 관리 UI를 표시한다.

현재:
```markdown
- If `--setup` flag is present **without** a task description:
  - Proceed to Steps 4~6 to ask all questions
  - Save answers as profile (Step 7.5)
  - Display: `💾 프로필이 업데이트되었습니다.`
  - **Stop here** — do NOT start a new session
```

변경:
```markdown
- If `--setup` flag is present **without** a task description:
  - Read `.claude/deep-work-profile.yaml` if it exists
  - If profile exists (v1 or v2): Show preset management UI (see Step 1.5d)
  - If profile does not exist: Proceed to Steps 4~6 to ask all questions, save as v2 profile (Step 7.5)
  - Display: `💾 프로필이 업데이트되었습니다.`
  - **Stop here** — do NOT start a new session
- If `--setup` flag is present **with** a task description:
  - Show preset management UI (Step 1.5d) → save → continue session with selected preset
- Otherwise (no `--setup`, task description is empty):
  - Ask the user: "작업 설명을 입력해주세요" using AskUserQuestion
  - Use the response as the task description
```

- [ ] **Step 3: Step 1.5c 전면 재작성 (v2 프로필 로드 로직)**

`commands/deep-work.md:69-87`의 Step 1.5c를 다음으로 교체:

```markdown
#### 1.5c. Load profile

Read `.claude/deep-work-profile.yaml` if it exists.

**If profile exists AND `--setup` is NOT set:**

1. Parse `version` field:
   - If `version` is `1`: **Auto-migrate to v2** — wrap existing `defaults.*` fields into `presets.default.*`, set `version: 2`, `active: default`. Overwrite the file. Display:
     ```
     🔄 프로필을 v2 형식으로 자동 업그레이드했습니다. (default 프리셋으로 변환)
     ```
   - If `version` is `2`: Proceed normally.
   - Otherwise: Display warning and treat as no profile:
     ```
     ⚠️ 프로필 버전이 호환되지 않습니다 (version: [N]). 기존 질문 흐름을 진행합니다.
     ```

2. **Preset selection** (v2 profile):
   - If `--profile=X` flag is set:
     - Look up `presets[X]`. If found, select it.
     - If not found: Display error and fall back to interactive selection:
       ```
       ⚠️ 프리셋 '[X]'을(를) 찾을 수 없습니다. 사용 가능한 프리셋 중에서 선택하세요.
       ```
   - If no `--profile` flag AND only 1 preset exists:
     - Auto-select the only preset.
   - If no `--profile` flag AND 2+ presets exist:
     - Use AskUserQuestion to let the user choose:
       ```
       🎯 프리셋을 선택하세요:
         1. dev — Solo / 기존 코드베이스 / Research부터
         2. quick — Solo / 기존 코드베이스 / Plan부터
         3. review — Team / 기존 코드베이스 / Research부터
       ```
       (Each option shows the preset name + key settings summary: team_mode / project_type / start_phase)

3. **Map selected preset fields to internal variables:**
   - `presets.<name>.team_mode` → `TEAM_MODE`
   - `presets.<name>.project_type` → `PROJECT_TYPE`
   - `presets.<name>.start_phase` → `START_PHASE`
   - `presets.<name>.git_branch` → `GIT_BRANCH`
   - `presets.<name>.model_routing.*` → `MODEL_ROUTING_*`
   - `presets.<name>.notifications.*` → `NOTIFICATIONS_*`

4. Apply flag overrides (if any): `--team`, `--zero-base`, `--skip-research`, `--no-branch` take precedence over preset values.

5. Display applied profile:
   ```
   ⚡ 프리셋 적용: [preset_name] — [team_mode] / [project_type] / [start_phase]부터 / [research]-[plan]-[implement]-[test]
   ```

6. Set `PROFILE_LOADED` = true, `SELECTED_PRESET` = preset name
7. **Skip Steps 4, 4-1, 4-2, 5, and 6** — use preset values directly

**If profile does NOT exist OR `--setup` IS set:**
1. Set `PROFILE_LOADED` = false
2. Continue to Steps 4~6 as normal (existing behavior)
```

- [ ] **Step 4: Step 1.5d 신규 추가 (프리셋 관리 UI)**

Step 1.5c 바로 뒤에 새로운 섹션 추가:

```markdown
#### 1.5d. Preset management UI (for `--setup`)

This UI is shown when `--setup` flag is used with an existing v2 profile.

1. Read `.claude/deep-work-profile.yaml` and parse `presets` keys.
2. If version is 1, auto-migrate to v2 first (same as Step 1.5c migration).
3. Display preset list using AskUserQuestion:
   ```
   ⚙️ 프리셋 관리

   현재 프리셋:
     1. dev ✏️ — Solo / existing / Research부터
     2. quick ✏️ — Solo / existing / Plan부터
     3. ➕ 새 프리셋 만들기

   편집하거나 새로 만들 프리셋을 선택하세요:
   ```

4. **If existing preset selected (edit):**
   - Display current values for each setting
   - Ask each question (Steps 4~6 flow) with current value as default
   - User can press Enter to keep existing value or type a new one
   - Save updated values back to the preset

5. **If "새 프리셋 만들기" selected:**
   - Ask for preset name using AskUserQuestion:
     ```
     📝 프리셋 이름을 입력하세요 (영문, 예: dev, quick, review):
     ```
   - Proceed through Steps 4~6 to collect all settings
   - Save as a new preset under the given name

6. After saving:
   - Display: `💾 프리셋 '[name]'이(가) 저장되었습니다.`
   - Update `active` field to the saved preset name
   - Set `PROFILE_LOADED` = true, `SELECTED_PRESET` = saved preset name
```

- [ ] **Step 5: Step 7 상태 파일에 `preset` 필드 추가**

`commands/deep-work.md:279-312`의 상태 파일 템플릿에 `preset` 필드 추가.

`team_mode:` 줄 바로 위에 추가:
```yaml
preset: "<SELECTED_PRESET or 'default'>"
```

- [ ] **Step 6: Step 7.5 프로필 저장 형식을 v2로 변경**

`commands/deep-work.md:326-352`의 Step 7.5를 수정:

현재:
```yaml
version: 1
created_at: "<current ISO timestamp>"
updated_at: "<current ISO timestamp>"

defaults:
  team_mode: "<selected team_mode>"
  ...
```

변경:
```yaml
version: 2
created_at: "<current ISO timestamp>"
updated_at: "<current ISO timestamp>"
active: "default"

presets:
  default:
    team_mode: "<selected team_mode>"
    project_type: "<selected project_type>"
    start_phase: "<selected start_phase>"
    git_branch: <true if branch was created, false otherwise>
    model_routing:
      research: "<selected>"
      plan: "main"
      implement: "<selected>"
      test: "<selected>"
    notifications:
      enabled: <true/false>
      channels: <copy from state file notifications.channels>
```

Display 메시지도 변경:
```
💾 프로필이 저장되었습니다 (default 프리셋). 다음 실행부터 이 설정이 자동 적용됩니다.
```

- [ ] **Step 7: 확인 메시지(Step 8)에 프리셋 이름 표시**

`commands/deep-work.md:360-407`의 확인 메시지 템플릿에서, `🤝 작업 모드:` 줄 위에 프리셋 정보 추가:

```
🎯 프리셋: [preset_name]
```

`PROFILE_LOADED`가 false이면 이 줄 생략.

- [ ] **Step 8: 커밋**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && git add commands/deep-work.md && git commit -m "feat(v3.3.3): 멀티 프리셋 Profile System (v2 형식, 자동 마이그레이션, --profile 플래그, --setup 관리 UI)"
```

---

## Task 5: commands/deep-resume.md — preset 필드 표시

**Files:**
- Modify: `commands/deep-resume.md`

- [ ] **Step 1: Step 1에서 preset 필드 추출 추가**

`commands/deep-resume.md:18`를 수정:

현재:
```markdown
Read `.claude/deep-work.local.md`. Extract `current_phase`, `work_dir`, `task_description`, `started_at`, `team_mode`, `plan_approved`, `test_retry_count`, and `max_test_retries` from the YAML frontmatter.
```

변경:
```markdown
Read `.claude/deep-work.local.md`. Extract `current_phase`, `work_dir`, `task_description`, `started_at`, `team_mode`, `plan_approved`, `test_retry_count`, `max_test_retries`, and `preset` from the YAML frontmatter.
```

- [ ] **Step 2: Step 3 표시 메시지에 프리셋 정보 추가**

`commands/deep-resume.md:83-97`의 표시 템플릿 수정:

`📂 작업 폴더:` 줄 뒤에 추가:
```
🎯 프리셋: [preset] (or 생략 if empty/not set)
```

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && git add commands/deep-resume.md && git commit -m "feat(v3.3.3): deep-resume에 프리셋 정보 표시"
```

---

## Task 6: 버전 업데이트 (plugin.json + package.json)

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `package.json`

- [ ] **Step 1: `.claude-plugin/plugin.json` 업데이트**

파일: `/Users/sungmin/.claude/plugins/deep-work/plugins/deep-work/.claude-plugin/plugin.json`

변경:
- `"version": "3.3.2"` → `"version": "3.3.3"`
- `"description"` → `"4-phase workflow with 3-tier Quality Gate, Multi-preset Profile System, Session Resume, and Checkpoint verification"`
- `"keywords"` 배열에 `"preset"`, `"multi-preset"`, `"profile-v2"` 추가

- [ ] **Step 2: `package.json` 업데이트**

파일: `/Users/sungmin/.claude/plugins/deep-work/plugins/deep-work/package.json`

변경:
- `"version": "3.3.2"` → `"version": "3.3.3"`
- `"description"` → `"Deep Work plugin for Claude Code - 4-phase workflow with Multi-preset Profile System, 3-tier Quality Gate (Plan Alignment + SOLID Review + Code Insight), PostToolUse file tracking, Stop hook"`

- [ ] **Step 3: 커밋**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && git add .claude-plugin/plugin.json package.json && git commit -m "chore(v3.3.3): 버전 업데이트 3.3.2 → 3.3.3"
```

---

## Task 7: 통합 검증

- [ ] **Step 1: 전체 파일 변경 확인**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && git log --oneline -6
```

6개 커밋이 있어야 함 (Task 1~6).

- [ ] **Step 2: JSON 파일 유효성 재확인**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && python3 -c "
import json
for f in ['.claude-plugin/plugin.json', 'package.json', 'deep-work-workflow-workspace/trigger-eval.json']:
    data = json.load(open(f))
    print(f'{f}: OK')
"
```

- [ ] **Step 3: 버전 일관성 확인**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && python3 -c "
import json, re
pkg = json.load(open('package.json'))['version']
plugin = json.load(open('.claude-plugin/plugin.json'))['version']
with open('skills/deep-work-workflow/SKILL.md') as f:
    skill = re.search(r'version: \"(.+?)\"', f.read()).group(1)
print(f'package.json: {pkg}')
print(f'plugin.json: {plugin}')
print(f'SKILL.md: {skill}')
assert pkg == plugin == skill == '3.3.3', f'Version mismatch! {pkg} / {plugin} / {skill}'
print('All versions match: 3.3.3 ✅')
"
```

- [ ] **Step 4: trigger-eval 최종 카운트 확인**

```bash
cd /Users/sungmin/.claude/plugins/deep-work/plugins/deep-work && python3 -c "
import json
data = json.load(open('deep-work-workflow-workspace/trigger-eval.json'))
true_count = sum(1 for d in data if d['should_trigger'])
false_count = sum(1 for d in data if not d['should_trigger'])
print(f'Total: {len(data)} (true: {true_count}, false: {false_count})')
assert len(data) >= 30, f'Expected 30+, got {len(data)}'
print('✅ Count target met')
"
```

- [ ] **Step 5: Obsidian 로드맵 문서에 v3.3.3 마일스톤 추가**

`/Users/sungmin/Library/CloudStorage/GoogleDrive-csm0825@gmail.com/내 드라이브/Obsidian/Personal Vault/Home/1. PROJECT/Programming/Deep-Work Plugin/Deep-Work 로드맵.md`

완료된 마일스톤 테이블에 행 추가:
```
| v3.3.3 | 2026-03-24 | 멀티 프리셋 Profile v2, SKILL.md 트리거 최적화 |
```

단기 섹션을 "완료"로 표시하거나 다음 단기 목표로 업데이트.
