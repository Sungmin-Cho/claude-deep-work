# Flag and profile choices

Parse actual arguments with the contained ${CLAUDE_PLUGIN_ROOT}/scripts/parse-deep-work-flags.js or flags parse route. Explicit arguments override stored choices; stored choices override fresh solo-adaptive defaults. Preserve main and concrete model/effort pins. --no-ask is setup-only; autonomous/internal continuation does not authorize external effects.

Read `${CLAUDE_PLUGIN_ROOT}/skills/shared/references/runtime-execution-spine.md` for the contained runtime commands and exact authority boundaries.
