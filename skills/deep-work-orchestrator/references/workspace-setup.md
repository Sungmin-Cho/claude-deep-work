# Workspace setup

Use session repository prepare in the authorized current-branch/new-branch/worktree mode. Use the returned state path/work_dir. Do not create an extra worktree when the user selected one. Necessary supported dependency preparation uses session environment explain then an explicitly authorized prepare digest.

Read `${CLAUDE_PLUGIN_ROOT}/skills/shared/references/runtime-execution-spine.md` for the contained runtime commands and exact authority boundaries.
