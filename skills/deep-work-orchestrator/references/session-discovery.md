# Session discovery

Use session context to find an existing task, or session initialize followed by session repository prepare for a new task. Preserve returned session identity and the user-selected worktree. Never invent timestamp IDs or write state by hand.

Read `${CLAUDE_PLUGIN_ROOT}/skills/shared/references/runtime-execution-spine.md` for the contained runtime commands and exact authority boundaries.
