# Source drift recovery

Validate current authority through session authority validate. Before any plan was bound, missing future artifacts is legitimate. After binding, stale source/Spec/plan/oracle evidence invokes the corresponding authenticated replan/recovery operation. Never clear the monotonic latch or copy an old PASS into new state.

Read `${CLAUDE_PLUGIN_ROOT}/skills/shared/references/runtime-execution-spine.md` for the contained runtime commands and exact authority boundaries.
