# Verification appropriate to the goal

Execute the approved verification plan through the registered runtime producers. Source code or a package script that looks like a test command is not automatically an admitted verifier. The selected strict or outcome basis determines which producer and evidence contract apply.

Run meaningful focused checks and the required relevant suite. Broaden or repeat when changes, failures or unresolved risks justify it. Preserve actual exit/output/timeout/overflow and supported-runtime identities. Never turn missing tool evidence into pass or not-applicable without policy grounds.

Test consumption uses authenticated receipts and owned gate-results. Retry only affected slices, preserving healthy siblings. Replan material contract changes through the runtime, then revalidate the new evidence. A passing Test phase does not itself finalize the user-requested repository outcome or publish the session receipt.

Use `${CLAUDE_PLUGIN_ROOT}/skills/shared/references/runtime-execution-spine.md` for Test/Finish commands and recovery. Report test arithmetic, machine trace and semantic goal acceptance separately; no percentage claims from unmeasured self-reports.
